openapi: 3.1.0
info:
  title: Action starter API
  version: 1.0.0
servers:
  - url: https://api.example.test
paths:
  /health:
    get:
      operationId: getHealth
      responses:
        '200':
          description: Health response
          content:
            application/json:
              schema:
                type: object
                required: [status]
                properties:
                  status:
                    type: string
