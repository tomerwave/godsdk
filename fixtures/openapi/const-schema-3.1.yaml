openapi: 3.1.0
info:
  title: Const schema regression
  version: 1.0.0
paths:
  /health:
    get:
      operationId: health
      responses:
        '200':
          description: Healthy
          content:
            application/json:
              schema:
                type: string
                const: ok
components:
  schemas:
    Version:
      type: integer
      const: 1
