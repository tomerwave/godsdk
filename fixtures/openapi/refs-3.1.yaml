openapi: 3.1.1
info:
  title: External references fixture API
  version: 1.0.0
paths:
  /users/{user_id}:
    get:
      operationId: getUser
      parameters:
        - $ref: './refs/models.yaml#/components/parameters/UserId'
      responses:
        '200':
          description: User
          content:
            application/json:
              schema:
                $ref: './refs/models.yaml#/components/schemas/User'
