openapi: 3.1.0
info:
  title: Untyped schema regression
  version: 1.0.0
paths:
  /value:
    get:
      operationId: getValue
      responses:
        '200':
          description: Arbitrary JSON value
          content:
            application/json:
              schema:
                description: Any JSON value is accepted
